- if searching?
  %h2= t('results_for', :scope => 'refinery.admin.search', :query => params[:search])
- if @blogs.any?
  .pagination_container
    =render :partial => 'blogs'
- else
  %p
    - if searching?
      =t('no_results', :scope=> 'refinery.admin.search')
    - else
      %strong
        =t('.empty_set')
        =t('.create_first', :link => t('new', :scope=>'refinery.yuzublog.admin.blog.actions'))
