- if searching?
  %h2= t('results_for', :scope => 'refinery.admin.search', :query => params[:search])
- if @posts.any?
  %h2 List of posts
  .pagination_container
    =render :partial => 'posts'
- else
  %p
    - if searching?
      =t('no_results', :scope=> 'refinery.admin.search')
    - else
      %strong
        =t('.empty_set')
        =t('.create_first', :link => t('new', :scope=>'refinery.yuzublog.admin.post.actions'))
